"""
🎯 الوظيفة: نصائح المساعدة
"""

import logging
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import ContextTypes

logger = logging.getLogger(__name__)


async def show_help_tips(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """عرض النصائح"""
    
    keyboard = [
        [
            InlineKeyboardButton("🔍 كيفية البحث", callback_data="tip_search"),
            InlineKeyboardButton("📥 كيفية التحميل", callback_data="tip_download")
        ],
        [
            InlineKeyboardButton("⚙️ إعدادات", callback_data="tip_settings"),
            InlineKeyboardButton("🆘 حل المشاكل", callback_data="tip_problems")
        ],
        [
            InlineKeyboardButton("📝 قوائس الحلقات", callback_data="tip_episodes"),
            InlineKeyboardButton("🔔 النشرات", callback_data="tip_notifications")
        ]
    ]
    
    await update.message.reply_text(
        """
📚 نصائح المساعدة:

اختر الموضوع الذي تريد معرفة المزيد عنه:
        """,
        reply_markup=InlineKeyboardMarkup(keyboard)
    )


async def handle_tips(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة النصائح"""
    
    try:
        callback_data = update.callback_query.data
        
        tips = {
            'tip_search': """
🔍 كيفية البحث:

1️⃣ البحث البسيط:
   /search سلمات
   
2️⃣ البحث بحلقة محددة:
   /search سلمات الحلقة 7

3️⃣ البحث بالجودة:
   /search سلمات 720p

4️⃣ البحث عن مسلسل:
   /series سلمات

💡 نصيحة: استخدم أسماء دقيقة للحصول على نتائج أفضل
            """,
            
            'tip_download': """
📥 كيفية التحميل:

1️⃣ ابحث عن المحتوى:
   /search اسم الفيلم

2️⃣ اختر النتيجة:
   اضغط على الزر 📥

3️⃣ اختر الجودة:
   اختر من الخيارات المتاحة

4️⃣ ابدأ التحميل:
   سيتم إرسال الملف مباشرة

⏱️ ملاحظة: الملفات الكبيرة قد تستغرق وقتاً
            """,
            
            'tip_settings': """
⚙️ الإعدادات:

🔄 أوضاع البحث:
   /search_dual - بحث مزدوج
   /search_local - بحث محلي
   /search_web - بحث في المواقع

💾 الإعدادات الأخرى:
   /settings - جميع الإعدادات

📱 لكل مستخدم إعدادات خاصة
            """,
            
            'tip_problems': """
🆘 حل المشاكل:

❌ الملف لا ينزل؟
   • جرب جودة أقل
   • تأكد من الإنترنت
   • جرب لاحقاً

❌ البحث لا يجد النتائج؟
   • استخدم أسماء صحيحة
   • جرب كلمات أخرى
   • تأكد من وجود المحتوى

❌ بطء في التحميل؟
   • المواقع قد تكون مشغولة
   • جرب وقت آخر
   • استخدم VPN

📞 للدعم: @support_bot
            """,
            
            'tip_episodes': """
📋 قوائم الحلقات:

كيفية رؤية جميع الحلقات:
   /series اسم المسلسل

المعلومات المتاحة:
   • اسم المسلسل
   • عدد الحلقات
   • إجمالي الحجم
   • الجودات المتاحة

اختيار الجودة:
   اضغط على الجودة المفضلة

تحميل الكل:
   /download_all اسم المسلسل
            """,
            
            'tip_notifications': """
🔔 النشرات:

تفعيل النشرات:
   /notifications اسم المسلسل

ستتلقى إشعارات عند:
   ✅ حلقة جديدة
   ✅ موسم جديد
   ✅ جودة جديدة

إيقاف النشرات:
   /stop_notifications اسم المسلسل

ملاحظة: النشرات تصل فوراً!
            """
        }
        
        tip_text = tips.get(callback_data, "لم أجد هذا الموضوع")
        
        await update.callback_query.answer("✅ تحديث النصيحة")
        
        await update.callback_query.edit_message_text(tip_text)
    
    except Exception as e:
        logger.error(f"❌ خطأ: {e}")